<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <h2> Edit a Historical Figure </h2>
    <form action="/figures/<%= @figure.id %>" method="post">
      <input type="hidden" name="_method" value="PATCH"/>
      <label for="figure_name"> Name: </label> <br>
      <input type="text" name="figure[name]" value="<%= @figure.name %>" id="figure_name"><br>

      <br>
      <label for="titles_checkbox"> Titles: </label><br>
      <input type="hidden" name="figure[title_ids][]" value="x">
      <% @titles.each do |title| %>
        <% if @figure_title_ids.include?(title.id) %>
          <input type="checkbox" name="figure[title_ids][]" value="<%= title.id %>" id="title_<%= title.id %>" checked> <%= title.name %> <br>
        <% else %>
          <input type="checkbox" name="figure[title_ids][]" value="<%= title.id %>" id="title_<%= title.id %>"> <%= title.name %> <br>
        <% end %>
      <% end %>
      <br>
      <label for="title_name"> Add A Title: </label> <br>
      <label for="title_name"> Name: </label>
      <input type="text" name="title[name]" id="title_name"><br>

      <br>
      <label for="landmark_id"> Associated Landmarks: </label><br>
      <input type="hidden" name="figure[landmark_ids][]" value="x">
      <% @landmarks.each do |landmark| %>
        <% if @figure_landmarks.include?(landmark) %>
          <input type="checkbox" name="figure[landmark_ids][]" value="<%= landmark.id %>" id="landmark_<%= landmark.id %>" checked> <%= landmark.name %> <br>
        <% else %>
          <input type="checkbox" name="figure[landmark_ids][]" value="<%= landmark.id %>" id="landmark_<%= landmark.id %>"> <%= landmark.name %> <br>
        <% end %>
      <% end %>
      <br>
      <label for="landmark_name"> Add A Landmark: </label> <br>
      <label for="landmark_name"> Name: </label>
      <input type="text" name="landmark[name]" id="landmark_name">
      <label for="landmark_year_completed"> Year Completed: </label>
      <input type="text" name="landmark[year_completed]" id="landmark_year_completed"><br>
      <br>
      <button type="submit"> Edit Figure </button>
    </form>
    <br>
    <br>
    <a href='/figures'> Go Back to Historic Figures </a> <br>
    <br>
    <a href='/landmarks'> Go to Historic Landmarks </a>
  </body>
</html>
